- [Admin E2E harness](e2e-admin-harness.md) — global-setup must write BOTH auth files (storageState + {token}); use the `admin-data-ready` sentinel instead of racing the dashboard polling loop.
- [Withdrawal tutorial video](withdrawal-tutorial-video.md) — live React animation exists in TWO synced copies (portal + standalone recorder); edit both. Copy is locale-driven via captions.ts.
- [AdminDashboard size budget](admin-dashboard-size-budget.md) — keep AdminDashboard.tsx under 512000 bytes or Babel logs a slow-compile deopt warning; extract leaf components/dialogs to `components/admin/`.
- [Admin source-assertion tests](admin-source-assertion-tests.md) — admin __tests__ assert on raw dashboard source; bound slices to the next decl, never `slice(idx, idx+N)` fixed windows.
- [SupportingDocs onActioned contract](supporting-docs-onactioned-contract.md) — onActioned fires in `finally` (even on PATCH failure); Tab double-fetches on mount; use matching caseId to dodge the zero-overlap confirm dialog in tests.
- [Admin RBAC design](admin-rbac-design.md) — role hierarchy, session validation change, priority routes gated, pre-existing portal-warning test failure note.
- [Community thread SEO permalinks](community-thread-seo-permalinks.md) — dynamic DB-backed pages need a numeric path regex kept in sync across client route, hreflang allowlist, and server prerender.
- [Admin sidebar panel localStorage](admin-sidebar-panel-localstorage.md) — CasesTab persists which sidebar panel is open; clear localStorage in beforeEach or tests flip panel-toggle clicks open→closed across runs.
- [EmailService mock factory](email-service-mock-factory.md) — all EmailService test mocks use a Proxy-based auto-stub factory (like storageMock) so a new method never needs matching updates across test files.
- [Post-merge alerting env var scoping](post-merge-alerting.md) — SLACK_WEBHOOK_URL/SMTP_* for post-merge.sh alerts live in Replit secrets, separate from the same-named GitHub Actions secrets used by CI alerts.
- [Portal session last-activity tracking](portal-session-activity-tracking.md) — last_activity_at is bumped centrally inside validateSession(), not per-route; new token consumers must route through it.
- [Sandbox dev-server backgrounding](sandbox-dev-server-backgrounding.md) — manual `nohup npm run dev &` dies between bash tool calls in this project's sandbox; use the "Start application" workflow (restart_workflow) for local e2e runs instead.
- [CI required-checks three-way sync](ci-required-checks-three-way-sync.md) — new CI jobs need required-checks.txt + docs/ci-checks.md + workflow YAML in lockstep; admin.ts env vars are read once at import (use vi.resetModules in tests).
- [Local dev DB case-volume perf ceiling](local-devdb-case-volume.md) — shared dev DB has thousands of real case rows; a component adding real fetches on mount needs test-timeout headroom in this sandbox.
- [New e2e spec CI registration](e2e-spec-ci-registration.md) — a new Playwright spec needs its own job in e2e-tests.yml plus entries in required-checks.txt, docs/ci-checks.md, and replit.md's CI list, or it silently never runs in CI.
- [Verify secrets live, not just presence](verify-secrets-live.md) — a secret existing (SMTP_*, webhook URLs, API keys) doesn't mean it's valid; trigger the real send/call once to confirm before declaring an alerting/notification task done.
- [Route rate-limit scan coverage](route-rate-limit-scan-coverage.md) — publicPostRateLimitScan.test.ts requires every server/routes/*.ts router file to be explicitly categorized (scanned or exempted-with-reason), not just the 5 originally hardcoded files.
- [Satisfaction-token single-use enforcement](satisfaction-token-nonce-store.md) — HMAC verification proves authenticity, not freshness; use a DB nonce-claim table (not in-memory Set) for anti-replay across autoscale instances.
- [Rate-limit window snapshot coverage](rate-limit-window-snapshot-coverage.md) — every `rateLimiter(..., {persistNamespace})` call site needs its own frozen-time window-duration test; a new limiter silently lacks one otherwise.
- [Admin context mock drift](admin-context-mock-drift.md) — a few admin tests hand-roll `AdminDashboardContextValue` with an `as` cast, bypassing tsc; grep for it after adding required context fields.
- [E2E bulk fixture seeding vs rate limiter](e2e-bulk-seeding-rate-limit.md) — seeding 100+ rows via an admin POST API in a spec can trip the generic per-path `/api` rate limiter even with a valid admin token; seed via direct SQL insert instead.
- [Rate limiter route-template keying](rate-limiter-route-template-keying.md) — per-route rate limiters must key on `req.route.path`, not the literal URL, or a varying `:id` gives an attacker a fresh bucket every time.
- [CasesTab confirm-threshold test override](casestab-confirm-threshold-test-override.md) — `vi.doMock` inside an `it()` can't override a statically-imported CasesTab's context; use a module-level mutable case-list variable instead.
- [429 body key mismatch client-side](rate-limit-429-body-key.md) — persistent rateLimiter() 429 bodies use `message`, not `error`; a client reading `err.error` silently falls back to a generic message and hides "you're rate limited" from the user.
- [active_visitors caseId FK gap](active-visitors-caseid-fk-gap.md) — deleteCase doesn't cascade active_visitors/visitor_history/chat_satisfaction_ratings; a case with chat/rating history 500s on delete.
